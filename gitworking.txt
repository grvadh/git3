git 
#git init
#get 
#

################ Git Branch ####################

#1. git branch #list all the branches and highlights the current branch
#2. git branch <branch_name> #creates a new branch
#3. git checkout <branch_name> # jums (i.e, checkout) to new branch

# 2 and 3 can be obtained from single command from the below command:
# git checkout -b <branch_name>

#5. git merge <branch_name> #This merges the code from <branch_name> to 
#the current branch

# mergint branch 1 and main